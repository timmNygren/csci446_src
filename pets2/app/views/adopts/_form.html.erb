<%= form_for(@adopt) do |f| %>
  <% if @adopt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adopt.errors.count, "error") %> prohibited this adopt from being saved:</h2>

      <ul>
      <% @adopt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.text_field :pet_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
