<%= form_for(@consider_adopt) do |f| %>
  <% if @consider_adopt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consider_adopt.errors.count, "error") %> prohibited this consider_adopt from being saved:</h2>

      <ul>
      <% @consider_adopt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.text_field :pet_id %>
  </div>
  <div class="field">
    <%= f.label :consider_id %><br>
    <%= f.text_field :consider_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
